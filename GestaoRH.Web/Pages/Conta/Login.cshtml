@page
@model GestaoRH.Web.Pages.Conta.LoginModel
@{
    ViewData["Title"] = "Login";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h3 class="card-title mb-4 fw-bold text-primary">
                        <i class="bi bi-box-arrow-in-right me-2"></i> Login
                    </h3>

                    @if (!string.IsNullOrWhiteSpace(Model.Erro))
                    {
                        <div class="alert alert-danger">@Model.Erro</div>
                    }

                    <form method="post">
                        <div class="mb-3">
                            <label asp-for="Login.Email" class="form-label">Email</label>
                            <input asp-for="Login.Email" class="form-control" />
                            <span asp-validation-for="Login.Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Login.Senha" class="form-label">Senha</label>
                            <input asp-for="Login.Senha" type="password" class="form-control" />
                            <span asp-validation-for="Login.Senha" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-page="Register" class="btn btn-outline-secondary">
                                <i class="bi bi-person-plus"></i> Criar conta
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-box-arrow-in-right"></i> Entrar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
}
